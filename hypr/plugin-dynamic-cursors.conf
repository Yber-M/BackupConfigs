plugin:dynamic-cursors {
    enabled = true
    mode = tilt

    rotate {
        length = 24   # tamaño del "stick" simulado en px — usa el tamaño real de tu cursor para que se vea más realista
        offset = 0.0  # rotación adicional en grados, normalmente no la tocas
    }
}
