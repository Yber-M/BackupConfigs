-- Reglas de ventanas y capas.
-- Migración desde windowrules.conf.

---- MATSHELL / GTK4 LAYER SHELL ----
hl.layer_rule({
    match = { namespace = "gtk4-layer-shell" },
    blur = true,
    ignore_alpha = 0.2,
})

hl.layer_rule({
    match = { namespace = "bar" },
    blur = true,
    ignore_alpha = 0.2,
})
